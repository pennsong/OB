@model OB.Models.Document

@{
    ViewBag.Name = "客户资料";
    ViewBag.Title = "删除";
}

<h1>@ViewBag.Name</h1>
<h2>@ViewBag.Title</h2>

<h3>您确定要删除此条记录么?</h3>

@Html.Action("Abstract", "Document", new { id = Model.Id })

<div>
    @using (Html.BeginForm("DeleteSave", "Document"))
    {
        @Html.AntiForgeryToken()
        @Html.Hidden("id", Model.Id)
        @Html.Hidden("returnUrl", @Url.Content((string)ViewBag.ReturnUrl))

        <input type="submit" value="确认删除" />
    }
    <a href="@Url.Content((string)ViewBag.ReturnUrl)">取消</a>
</div>

@{
    string action = ((RouteValueDictionary)ViewBag.RV).Where(a => a.Key == "actionAjax").Select(a => a.Value).SingleOrDefault().ToString();
    string returnRoot = ((RouteValueDictionary)ViewBag.RV).Where(a => a.Key == "returnRoot").Select(a => a.Value).SingleOrDefault().ToString();

    ViewBag.Title = "客户列表";
    AjaxOptions ajaxOpts = new AjaxOptions
    {
        UpdateTargetId = "AjaxBody",
        Url = Url.Action(action),
    };
    
}

<h2>@ViewBag.Title</h2>

@using (Ajax.BeginForm(ajaxOpts))
{
    <table>
        <tr>
            <td>
                <span class="searchLabel">关键词</span>
            </td>
            <td></td>
        </tr>
        <tr>
            @Html.Hidden("returnRoot", returnRoot)
            @Html.Hidden("actionAjax", action)
            <td>
                @Html.TextBox("keyword")
            </td>
            <td>
                <button type="submit">查询</button>
            </td>
        </tr>
    </table>
}
<div id="AjaxBody">
    @Html.Action(action, (RouteValueDictionary)(ViewBag.RV))
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
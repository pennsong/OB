@model OB.Models.ClientPensionCityDocument

@{
    ViewBag.Name = "客户社保城市";
    ViewBag.Title = "删除";
}

<h1>@ViewBag.Name</h1>
<h2>@ViewBag.Title</h2>

<h3>您确定要删除此条记录么?</h3>

@Html.Action("Abstract", "ClientPensionCityDocument", new { id = Model.Id })

<div>
    @using (Html.BeginForm("DeleteSave", "ClientPensionCityDocument"))
    {
        @Html.AntiForgeryToken()
        @Html.Hidden("id", Model.Id)
        @Html.Hidden("returnUrl", @Url.Content((string)ViewBag.ReturnUrl))

        <input type="submit" value="确认删除" />
    }
    <a href="@Url.Content((string)ViewBag.ReturnUrl)">取消</a>
</div>
